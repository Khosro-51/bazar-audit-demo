# Bazar Audit — Quant Terminal theme
# فقط بخش theme — هیچ تنظیم server/port اینجا نیست؛ پورت همان پیش‌فرض 8501 می‌ماند.

[theme]
base = "dark"
primaryColor = "#00E5A0"
backgroundColor = "#07090C"
secondaryBackgroundColor = "#0D1117"
textColor = "#C9D4DF"
font = "sans serif"
