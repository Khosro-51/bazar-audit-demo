این دو مورد الان P0 قبل از ارائه به کاربر واقعی هستند:

کاربر نباید JSON ببیند. JSON برای ماست، نه برای آدمی که فقط می‌خواهد بفهمد چرا حسابش خونریزی دارد.
ایمیل تکراری نباید دوباره دسترسی/رمز/آپلود بگیرد. اگر با همان ایمیل دوباره وارد شد و سیستم دوباره راه داد، یعنی دروازه نداریم، فقط یک پرده نمایشی داریم. پرده هم جلوی کاربران کنجکاو زیاد دوام نمی‌آورد.

حکم من:

قبل از ادامه، باید دو ماژول اضافه شود
1. Human Report Export

یک دکمه:

Generate Full Statement Analysis Report

خروجی:

گزارش خوانا داخل خود صفحه
دکمه دانلود HTML یا PDF
JSON فقط داخل بخش مخفی Developer / Debug

برای نسخه سریع، HTML Report بهتر از PDF است، چون فارسی/عربی و RTL در PDF دردسر فونت و layout دارد. PDF را بعدش می‌سازیم. ولی کاربر باید یک فایل قابل فهم بگیرد، نه جعبه سیاه JSON که فقط توسعه‌دهنده‌ها با آن احساس زنده‌بودن می‌کنند.

2. Access + Email Usage Gate

قفل‌ها باید این‌طور باشند:

Access Code Gate
↓
Sample Demo آزاد بعد از ورود
↓
Private Upload فقط با ایمیل
↓
هر ایمیل فقط یک audit/report
↓
ایمیل تکراری = block
↓
IP hash فقط برای abuse detection

نکته مهم: اگر روی Streamlit Cloud واقعاً می‌خواهی ایمیل تکراری را بگیری، beta_usage.json کافی نیست. روی Cloud ممکن است فایل local پایدار نباشد. برای تست خیلی کوچک شاید جواب بدهد، ولی برای کنترل واقعی باید Supabase یا دیتابیس مشابه داشته باشیم. وصله‌پینه‌ی local برای دروازه، مثل قفل‌زدن به درِ بدون دیوار است؛ نیت خوب، نتیجه طنز.

نکته مهم آخر:
اگر واقعاً می‌خواهی «با هر ایمیل فقط یک بار» قابل اتکا باشد، باید از همین حالا Supabase را جدی بگیریم. beta_usage.json برای تست محلی بد نیست، ولی روی Cloud قابل اعتماد نیست. برای demo بسته بین چند نفر شاید زنده بماند، اما برای هر چیزی که بوی محصول واقعی بدهد، دیتابیس لازم است. اینجا دیگر جای طناب و چسب نیست.

این پرامپت دقیقه بده کلود


Access Code Gate
↓
Sample Demo آزاد بعد از ورود
↓
Private Upload فقط با ایمیل
↓
هر ایمیل فقط یک audit/report
↓
ایمیل تکراری = block
↓
IP hash فقط برای abuse detection
